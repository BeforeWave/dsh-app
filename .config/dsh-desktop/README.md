# DS Harness desktop config

Location:

```text
~/.config/dsh-desktop/
```

Files:

```text
env.sh
healthcheck.sh
pre-start.d/
post-ready.d/
commands.d/
```

User files can override built-in defaults with the same filename and can add new command scripts without rebuilding the app.
